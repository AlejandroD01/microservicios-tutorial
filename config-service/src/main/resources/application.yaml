server:
  port: 8081
spring:
  cloud: 
    config:
      server:
        git:
          default-label: circuit-breaker
          uri: https://github.com/AlejandroD01/microservicios-tutorial
          search-paths: config-data
  application:
    name: config-service